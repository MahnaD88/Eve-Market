# Current GPT instructions — awaiting export

The custom GPT's saved instructions are not present in this repository and have
not been captured. Replace this placeholder with the verbatim instructions from
the GPT editor when supplied by the owner. No instructions have been inferred.

The registered Action schema, authentication settings, and enabled operations
also require verification against the GPT editor. Backend behavior alone cannot
establish which operations the GPT currently exposes.
